workflows:
  android-apk:
    name: Android APK

    environment:
      java: 17

    scripts:
      - chmod +x gradlew
      - ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
